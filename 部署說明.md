# 韓國周邊成本計算器 — 部署說明

## 架構
- **前端**：`index.html`（GitHub Pages 免費架設）
- **後端 API**：Google Apps Script（免費），抓商品頁 + 韓翻中 + 讀寫你的 Google 試算表
- **資料庫**：你現有的 Google 試算表新增一個「批次資料」tab

---

## 第一步：建立後端 API（約 5 分鐘）

1. 開啟 https://script.google.com/home → 點「+ 新增專案」
2. 把 `apps-script-backend.gs` 的內容全部貼進編輯器，取代原本的程式碼
3. 按 💾 儲存（試算表 ID 已寫在檔案開頭，不用改）
4. 上方選「部署 → 新增部署」
   - 類型選「**網頁應用程式**」
   - 「執行身分」：**我**
   - 「誰可以存取」：**所有人**
   - 按「部署」→ 授權（選你的 Google 帳號 → 進階 → 允許）
5. 複製產生的 **Web App 網址**（`https://script.google.com/macros/s/.../exec`）

## 第二步：設定前端

1. 打開網站 → 「設定」tab → 貼上 Web App 網址 → 儲存
2. 完成！之後就能貼網址抓商品、算成本、發佈批次。

## 第三步：架到 GitHub Pages

```bash
cd merch-site
git init && git add index.html && git commit -m "init"
# 在 GitHub 建立一個 repo（例如 merch-site）後：
git remote add origin https://github.com/<你的帳號>/merch-site.git
git push -u origin main
```
然後 GitHub repo → Settings → Pages → Source: `main` 分支 `/ (root)` → Save。
網址會是 `https://<你的帳號>.github.io/merch-site/`

---

## 成本公式（與你試算表 Loppy 聯名一致）

```
折算台幣   = 韓幣 × 匯率 ÷ 100      （匯率 = TWD per 100 KRW，如 4.75）
刷卡手續費 = 折算台幣 × 1.5%
預估成本   = 折算台幣 + 刷卡手續費
台幣訂價   = 預估成本 × (1+建議加成%)，四捨五入到十位（可手動改）
```

## 已知限制
- 抓取靠 og:image / og:title / 價格 meta，偶爾抓不到時可手動補欄位
- 翻譯用 Google 免費端點，若失效標題會保留韓文原文，仍可手動輸入中文
